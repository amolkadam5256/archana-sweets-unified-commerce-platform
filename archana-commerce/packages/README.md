# Shared Packages

TypeScript packages consumed by `apps/website`, `apps/dashboard`, and `apps/mobile`.

## Dependency Order

```
types → constants, validators, utils → hooks → ui
```

## Package Rules

See [Dependency Map](../../docs/architecture/DEPENDENCY_MAP.md) for import restrictions.

## Implementation Status

Package scaffolding begins in Phase 1.2.
